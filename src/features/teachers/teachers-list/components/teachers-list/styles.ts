import { styled } from '@anyclazz/ui';

export const TeachersListContainer = styled.div`
    display: flex;
    flex-direction: column;

    padding: ${({ theme }) => theme.spacings[32]}
`;

export const StyledList = styled.div`
    
`;