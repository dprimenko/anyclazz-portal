# API Configuration
API_URL=https://api.tudominio.com/api/v1

# Keycloak Configuration
KEYCLOAK_ISSUER=https://keycloak.tudominio.com/realms/anyclazz
KEYCLOAK_CLIENT_ID=anyclazz-app
KEYCLOAK_CLIENT_SECRET=tu-client-secret-aqui

# Auth Secret (generar uno único con: openssl rand -base64 32)
AUTH_SECRET=genera-un-secret-aleatorio-aqui

# Server Configuration (opcional, Dokploy puede configurar esto)
HOST=0.0.0.0
PORT=4321
NODE_ENV=production
